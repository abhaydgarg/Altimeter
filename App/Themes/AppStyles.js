import Colors from './Colors';

const AppStyles = {
  screen: {
    mainContainer: {
      flex: 1,
      backgroundColor: Colors.transparent
    },
    container: {
      flex: 1,
      backgroundColor: Colors.transparent
    }
  }
};

export default AppStyles;
